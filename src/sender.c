#include "sender.h"

static int _sender_data_is_valid(char *sender, char *destination, char *message);
static int _sender_send_message(char *sender, char *destination, char *message);


int sender_send_data(char *sender, char *destination, char *message)
{
    
}

static int _sender_data_is_valid(char *sender, char *destination, char *message)
{

}

static int _sender_send_message(char *sender, char *destination, char *message)
{

}